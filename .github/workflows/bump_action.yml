on:
    pull_request:
        types:
            - closed

jobs:
    if_merged:
        if: github.event.pull_request.merged == true
        runs-on: ubuntu-latest
        steps:
        - name: Bump beta version
          working-directory: ./
          run: ./.github/workflows/bump_version.sh ./package version.txt